import React from 'react'
import ReactLoading from 'react-loading'

export const Loading = () => {
    return (
        <ReactLoading type='spinningBubbles' color='blue' width={50} height={50} />
    )
}

export default Loading;
